import React, { Component } from 'react';

export default class Filter {

  

  render () {
    const forMan = newData.filter(category => category.category_id) // category_id 1
    const forWoman = neData.filter(category => category.category_id)
    return
  }
}
